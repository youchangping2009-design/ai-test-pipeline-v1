# Runtime Work Item Fixture

这是 Harness 单元测试使用的自包含匿名工作项，身份为 `FIXTURE / FIXTURE-RUNTIME-001`。

- 仅用于验证 Case Plan、角色运行、并行 Reviewer 和隔离发布行为。
- 不代表正式业务需求、默认验收项目或质量结论。
- 测试不得回退到 `assets/projects/` 下的业务工作项读取输入。
