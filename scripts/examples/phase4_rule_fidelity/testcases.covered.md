# 页面：示例配置页
## 板块：添加弹窗
| 用例编号 | 所属模块 | 所属功能点 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 标签 | 测试类型 | 备注 |
|---------|---------|-----------|---------|---------|---------|---------|-------|------|---------|------|
| DEMO-CONFIG-MOD-ADMIN-FN-001 | 示例配置模块 | 规则保真示例 | 验证展示天数在display_type=after_days时必填 | 已进入示例配置页并打开添加弹窗，展示类型已切换为`after_days` | 1. 保持`display_type=after_days`<br>2. 不填写`display_days`直接点击保存<br>3. 观察页面提示 | 1. `display_days`在`display_type=after_days`时被判定为必填<br>2. 未填写`display_days`时禁止保存<br>3. 页面明确提示需要补充展示天数 | P1 | AI-UI用例,测试必测 | 功能 | 承接 conditional_required |
| DEMO-CONFIG-MOD-ADMIN-FN-002 | 示例配置模块 | 规则保真示例 | 验证展示天数在display_type=after_days时展示且可编辑 | 已进入示例配置页并打开添加弹窗 | 1. 先选择`display_type!=after_days`<br>2. 再切换为`display_type=after_days`<br>3. 观察`display_days`显隐与编辑状态 | 1. `display_type!=after_days`时页面不展示`display_days`<br>2. `display_type=after_days`时页面展示`display_days`<br>3. `display_type=after_days`时`display_days`可编辑 | P1 | AI-UI用例,测试必测 | 功能 | 承接 conditional_visibility / conditional_editability |
| DEMO-CONFIG-MOD-ADMIN-FN-003 | 示例配置模块 | 规则保真示例 | 验证活动链接在jump_type=activity_center时只读 | 已进入示例配置页并打开添加弹窗，页面存在活动链接输入区域 | 1. 将`jump_type`切换为`activity_center`<br>2. 尝试编辑`link_url`字段<br>3. 观察字段编辑结果 | 1. `jump_type=activity_center`时`link_url`为只读状态<br>2. 用户无法修改`link_url`内容<br>3. 只读行为与规则一致 | P1 | AI-UI用例,测试必测 | 功能 | 承接 conditional_readonly |
| DEMO-CONFIG-MOD-ADMIN-BD-001 | 示例配置模块 | 规则保真示例 | 验证活动名称最大长度为20 | 已进入示例配置页并打开添加弹窗 | 1. 输入20个字符到`activity_name`<br>2. 再输入21个字符<br>3. 观察保存结果 | 1. 输入20个字符时可继续保存<br>2. 输入21个字符时触发长度校验<br>3. 长度上限保持为20 | P1 | AI-UI用例,测试必测 | 边界 | 承接 value_constraint max_length=20 |
| DEMO-CONFIG-MOD-ADMIN-BD-002 | 示例配置模块 | 规则保真示例 | 验证展示天数仅允许1到99之间的整数 | 已进入示例配置页并打开添加弹窗，展示类型已切换为`after_days` | 1. 输入`0`、`100`、`1.5`分别尝试保存<br>2. 输入`1`和`99`分别尝试保存<br>3. 观察结果 | 1. `0`和`100`不满足`min=1`、`max=99`<br>2. `1.5`不满足`integer_only=true`<br>3. `1`和`99`作为边界整数可被接受 | P1 | AI-UI用例,测试必测 | 边界 | 承接 value_constraint min/max/integer_only |
| DEMO-CONFIG-MOD-ADMIN-DV-001 | 示例配置模块 | 规则保真示例 | 验证选择活动仅展示状态=发布的数据并按活动创建时间倒序排序 | 系统中存在已发布和未发布活动 | 1. 打开`selected_activity`候选列表<br>2. 核对候选活动状态<br>3. 核对候选列表排序和展示格式 | 1. 候选数据来源于`活动中心渠道=小程序的活动`<br>2. 候选列表仅包含`状态=发布`的数据<br>3. 候选项按`按活动创建时间倒序`排序，并按`id+活动名称`格式展示 | P1 | AI-API用例,开发必测 | 数据校验 | 承接 data_source_constraint |
