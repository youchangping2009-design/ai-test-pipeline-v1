# 页面：示例配置页
## 板块：添加弹窗
| 用例编号 | 所属模块 | 所属功能点 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 标签 | 测试类型 | 备注 |
|---------|---------|-----------|---------|---------|---------|---------|-------|------|---------|------|
| DEMO-CONFIG-MOD-ADMIN-FN-001 | 示例配置模块 | 规则型字段示例 | 验证展示天数在display_type=after_days时必填 | 已进入示例配置页并打开添加弹窗，展示类型已切换为`after_days` | 1. 保持`display_type=after_days`<br>2. 不填写`display_days`直接点击保存<br>3. 观察保存结果与字段提示 | 1. `display_days`在`display_type=after_days`时被判定为必填<br>2. 未填写`display_days`时禁止保存<br>3. 页面明确提示需要补充展示天数 | P1 | AI-UI用例,测试必测 | 功能 | 来源结构：fields.required_when=`display_type=after_days` |
| DEMO-CONFIG-MOD-ADMIN-FN-002 | 示例配置模块 | 规则型字段示例 | 验证展示天数在display_type=after_days时展示 | 已进入示例配置页并打开添加弹窗 | 1. 先选择`display_type!=after_days`观察页面<br>2. 再切换为`display_type=after_days`<br>3. 对比`display_days`字段显隐变化 | 1. `display_type!=after_days`时页面不展示`display_days`<br>2. 切换为`display_type=after_days`后页面展示`display_days`输入框<br>3. 字段显隐与`visible_when`规则一致 | P1 | AI-UI用例,测试必测 | 功能 | 来源结构：fields.visible_when=`display_type=after_days` |
| DEMO-CONFIG-MOD-ADMIN-FN-003 | 示例配置模块 | 规则型字段示例 | 验证活动链接在jump_type=activity_center时只读 | 已进入示例配置页并打开添加弹窗，页面存在活动链接输入区域 | 1. 将`jump_type`切换为`activity_center`<br>2. 尝试编辑`link_url`字段<br>3. 观察字段编辑结果 | 1. `jump_type=activity_center`时`link_url`为只读状态<br>2. 用户无法修改`link_url`内容<br>3. 只读行为与`readonly_when`规则一致 | P1 | AI-UI用例,测试必测 | 功能 | 来源结构：rules.conditional_readonly，field=`link_url` |
| DEMO-CONFIG-MOD-ADMIN-BD-001 | 示例配置模块 | 规则型字段示例 | 验证活动名称最大长度为20 | 已进入示例配置页并打开添加弹窗 | 1. 在`activity_name`输入20个字符并尝试保存<br>2. 再输入21个字符并尝试保存<br>3. 观察页面校验结果 | 1. 输入20个字符时允许保存或继续下一步<br>2. 输入21个字符时触发长度校验，不允许按合法值保存<br>3. 长度上限保持为20 | P1 | AI-UI用例,测试必测 | 边界 | 来源结构：fields.max_length=20 |
| DEMO-CONFIG-MOD-ADMIN-BD-002 | 示例配置模块 | 规则型字段示例 | 验证展示天数仅允许1到99之间的整数 | 已进入示例配置页并打开添加弹窗，展示类型已切换为`after_days` | 1. 输入`0`、`100`、`1.5`分别尝试保存<br>2. 输入`1`和`99`分别尝试保存<br>3. 观察各输入值的校验结果 | 1. `0`和`100`不满足`min=1`、`max=99`，不能按合法值保存<br>2. `1.5`不满足`integer_only=true`，不能按合法值保存<br>3. `1`和`99`作为边界整数可被接受 | P1 | AI-UI用例,测试必测 | 边界 | 来源结构：fields.min=1，fields.max=99，fields.integer_only=true |
| DEMO-CONFIG-MOD-ADMIN-DV-001 | 示例配置模块 | 规则型字段示例 | 验证选择活动候选数据按状态=发布过滤并按活动创建时间倒序排序 | 系统中存在已发布和未发布活动，且已进入示例配置页添加弹窗 | 1. 打开`selected_activity`候选列表<br>2. 核对候选活动状态<br>3. 核对候选列表排序和展示格式 | 1. 候选数据来源于`活动中心渠道=小程序的活动`<br>2. 候选列表仅包含`状态=发布`的数据，不展示未发布活动<br>3. 候选项按`活动创建时间倒序`排序，并按`id+活动名称`格式展示 | P1 | AI-API用例,开发必测 | 数据校验 | 来源结构：fields.data_source / filter / display / order_by |
| DEMO-CONFIG-MOD-ADMIN-FL-001 | 示例配置模块 | 规则型字段示例 | 验证按天展示场景下填写合法展示天数并选择已发布活动后保存结果正确 | 已进入示例配置页并打开添加弹窗，存在至少1个已发布活动候选项 | 1. 选择`display_type=after_days`<br>2. 输入合法整数`display_days=7`并确认字段可编辑<br>3. 从候选列表选择按`状态=发布`过滤且按`活动创建时间倒序`展示的活动后点击保存 | 1. `display_days`在`after_days`场景下展示且必填，输入`7`满足1到99之间的整数约束<br>2. 活动候选列表仅展示已发布活动，且按创建时间倒序排列<br>3. 点击保存后页面不再提示必填、边界或候选数据错误，新增配置记录按输入值落盘并出现在结果列表中 | P0 | 核心链路,AI-UI用例,测试必测 | 流程验证 | 来源 Flow：FLOW-001；组合场景：required_when + visible_when + min/max/integer_only + data_source/filter/order_by |
