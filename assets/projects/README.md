# Projects

业务项目资产放在 `assets/projects/<PROJECT_CODE>/`。

当前仓库仅保留 `WX-YGJ/work_items/PT083` 作为 strict gate 与 quality baseline 的正向样例。其他项目工作项请在本地或业务仓自行初始化，不建议把大量历史实战资产长期提交进流程仓。

初始化命令见根目录 `README.md` 与 `docs/operating_sop.md`。
